'use client';
import Link from 'next/link'; import {usePathname} from 'next/navigation'; import {LayoutDashboard,Bot,Target,FlaskConical,Landmark,Brain,Archive,ShieldCheck,Settings} from 'lucide-react';
const links=[['/','Command',LayoutDashboard],['/agents','Agents',Bot],['/opportunities','Opportunities',Target],['/experiments','Experiments',FlaskConical],['/revenue','Revenue',Landmark],['/intelligence','Intelligence',Brain],['/graveyard','Graveyard',Archive],['/approvals','Approvals',ShieldCheck],['/settings','Settings',Settings]] as const;
export function Sidebar(){const p=usePathname();return <aside className="sidebar"><div className="brand">Revenue<b>OS</b><div className="eyebrow" style={{marginTop:5}}>V1 / OPERATING SYSTEM</div></div><nav className="nav" aria-label="Primary navigation">{links.map(([href,label,Icon])=><Link href={href} key={href} className={p===href||(href!=='/'&&p.startsWith(href))?'active':''}><Icon size={16}/>{label}</Link>)}</nav><div className="desktop-only" style={{position:'absolute',bottom:22,left:24,right:24}}><div className="card card-pad"><div className="eyebrow">System status</div><div style={{marginTop:8,fontWeight:700}}>Local simulation</div><div className="sub" style={{fontSize:12,marginTop:5}}>Wallet rules enforced · agents are demos</div></div></div></aside>}

'use client';
import {useMemo,useState} from 'react'; import {useRouter} from 'next/navigation'; import {agents} from '@/lib/data'; import {Header,Status} from '@/components/ui'; import {money} from '@/lib/finance';
export default function Agents(){const router=useRouter();const [filter,setFilter]=useState('all');const rows=useMemo(()=>agents.filter(a=>filter==='all'||a.status===filter),[filter]);return <><Header eyebrow="Autonomous workforce" title="Agents"><button className="primary" onClick={()=>alert('Agent creation is local-only in V1. Connect Supabase to persist a new agent.')}>New agent</button></Header><div className="card card-pad" style={{marginBottom:14,display:'flex',justifyContent:'space-between',gap:12,flexWrap:'wrap'}}><span className="sub">Each agent has a mandate, capital ceiling, heartbeat, and human-controlled survival review.</span><div style={{display:'flex',gap:7}}>{['all','active','paused'].map(x=><button key={x} onClick={()=>setFilter(x)} className={filter===x?'primary':''}>{x[0].toUpperCase()+x.slice(1)}</button>)}</div></div><div className="card card-pad scroll"><table><thead><tr><th>Agent</th><th>Status</th><th>Survival</th><th>Capital</th><th>Revenue</th><th>Heartbeat</th></tr></thead><tbody>{rows.map(a=><tr className="clickable" key={a.id} onClick={()=>router.push(`/agents/${a.id}`)}><td><b>{a.name}</b><div className="sub" style={{fontSize:12}}>{a.mandate}</div></td><td><Status value={a.status}/></td><td><b className={a.score<55?'warning':'positive'}>{a.score}</b><span className="sub"> / 100</span></td><td>{money(a.spent)} <span className="sub">/ {money(a.budget)}</span></td><td className="positive">{money(a.revenue)}</td><td className="sub">{a.lastHeartbeat}</td></tr>)}</tbody></table></div></>}

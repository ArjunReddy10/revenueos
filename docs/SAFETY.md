# Safety
No client-side secret exists. No UI simulates an external payment, launch, or transaction success. Spend above configured policy becomes an approval. Production actions require authenticated user, workspace policy check, idempotency key, immutable event record, and explicit external provider response. Avoid personal-data targeting without consent and retain evidence provenance.

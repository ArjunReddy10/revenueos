# Economics
Money uses `numeric(14,2)` in the ledger; posted inflow minus posted outflow is net revenue. Pending entries never alter recognized totals. Opportunity expected value is value × probability, not booked revenue. Agent revenue is attributed only from posted entries. The survival score helps allocate attention, while budgets and approval thresholds remain hard guardrails.
